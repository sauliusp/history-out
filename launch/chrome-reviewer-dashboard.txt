No login, payment or credentials required. Use a normal profile with browsing history; visit a few public pages if empty. Click HistoryOut, choose Today, then Preview. Export CSV, JSON and HTML locally. Try custom dates, columns, search, website filters and One row per URL. Save a named view, reopen and restore it. Fresh installs open Welcome once; updates from 1.0.1 open the changelog once. History stays local. No new permissions, remote code or history uploads.
