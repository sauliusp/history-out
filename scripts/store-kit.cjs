/* Fresh store compositions built around actual product captures and the original brand. */
const fs = require('node:fs/promises');
const path = require('node:path');
const crypto = require('node:crypto');
const { execFileSync } = require('node:child_process');
const { chromium } = require('playwright');
let sharp;
try { sharp = require('sharp'); } catch {
  sharp = require(path.join(require('node:os').homedir(),'.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/sharp'));
}
const { startServer } = require('./qa-lib.cjs');
const ROOT = path.resolve('launch/store-kit');
const ASSETS = path.resolve('launch/assets');
const sha = data => crypto.createHash('sha256').update(data).digest('hex');
const escape = s => s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/"/g,'&quot;');
const base = `<style>*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden}body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;color:#152d49;background:#f4f6fa}img{display:block}.art{position:relative;width:100%;height:100%;overflow:hidden;background:#f4f6fa}.brand{position:absolute;left:62px;top:41px;width:179px;height:auto}.kicker{font-size:13px;letter-spacing:2px;line-height:1.5;text-transform:uppercase;font-weight:750;color:#687f9b}h1{font-size:64px;line-height:1.04;letter-spacing:-3.3px;margin:20px 0 24px;font-weight:760}h1 b{color:#2479e0;font-weight:inherit}.copy{position:absolute;left:62px;top:214px;width:497px}p{font-size:22px;line-height:1.48;color:#5a6e88;margin:0;max-width:445px}.proof{display:flex;gap:11px;flex-wrap:wrap;margin-top:30px}.proof span{border:1px solid #d7e0eb;border-radius:8px;background:#fff;padding:10px 13px;font-size:15px;color:#345576;font-weight:600}.foot{position:absolute;left:62px;bottom:49px;font-size:14px;color:#59718e}.note{margin-top:12px;font-size:11px;color:#7a8b9f}.rail{position:absolute;right:0;top:0;width:609px;height:100%;background:#e6edf7;border-left:1px solid #dbe3ef}.frame{position:absolute;right:39px;top:28px;width:548px;height:744px;border:1px solid #c6d3e4;border-radius:16px;overflow:hidden;background:#fff;box-shadow:0 18px 35px #25456516}.bar{height:35px;display:flex;align-items:center;gap:5px;background:#fff;border-bottom:1px solid #e3e9f2;padding:0 13px;font-size:10px;letter-spacing:.2px;color:#78889e}.bar i{width:6px;height:6px;border-radius:50%;background:#d3dce9}.bar span{margin:auto}.capture{height:708px;display:flex;align-items:flex-start;justify-content:center;background:#f5f7fb;overflow:hidden}.capture img{width:504px;height:auto}.capture.stacked{display:block}.capture.stacked img{width:504px;margin:0 auto}.capture.stacked img:first-child{margin:10px auto 8px}.wide .rail{width:696px}.wide .copy{width:447px}.wide h1{font-size:61px}.wide p{max-width:400px}.wide .frame{width:671px;right:24px}.wide .capture img{width:671px;height:auto}.export-stack,.custom-stack{width:100%;padding:20px}.export-stack>img,.custom-stack .dates{width:100%;height:auto}.export-stack .download{margin:16px 0 24px}.result-label{font-size:12px;color:#506b88;padding:13px 15px;background:#fff;border:1px solid #dce5ef;border-radius:10px 10px 0 0}.file-crop{height:322px;overflow:hidden;border:1px solid #dce5ef;border-top:0;border-radius:0 0 10px 10px}.file-crop img{width:100%;height:auto}.column-area{display:flex;gap:20px;margin:20px 15px;align-items:flex-start}.column-area>div{padding-top:22px;flex:1}.column-area strong{font-size:25px;line-height:1.12;letter-spacing:-.7px}.column-area span{display:block;font-size:14px;color:#6a7e95;line-height:1.5;margin-top:14px}.column-area img{width:240px!important;height:auto;border-radius:10px;border:1px solid #dce5ef}.promo{background:#122c48}.promo:before{content:'';position:absolute;inset:0;background:radial-gradient(ellipse at 80% 5%,#2479e080,transparent 70%)}.promo .plaque{position:absolute;background:#fff;border:1px solid #d8e3f1;border-radius:20px;box-shadow:0 14px 35px #0002}.promo .plaque img{width:100%;height:100%;object-fit:contain}.route{position:absolute;border:1px solid #ffffff2b;border-radius:50%}.route:after{content:'';position:absolute;right:9%;top:12%;width:10px;height:10px;border-radius:50%;background:#80b5f4;box-shadow:0 0 0 8px #80b5f412}.format{position:absolute;background:#fff;border:1px solid #d6e0ee;border-radius:12px;box-shadow:0 12px 22px #0002;display:flex;align-items:center;justify-content:center;color:#2479e0;font-weight:750;letter-spacing:1.5px}.format:after{content:'';position:absolute;left:15%;right:15%;bottom:22%;height:3px;background:#e7effa;box-shadow:0 -9px #e7effa}.small .plaque{left:94px;top:58px;width:251px;height:124px;padding:19px 24px}.small .route{left:-75px;top:-58px;width:480px;height:360px;transform:rotate(-19deg)}.small .format{width:53px;height:68px;top:187px;font-size:11px;align-items:flex-start;padding-top:13px;border-radius:7px}.small .format.csv{left:124px;transform:rotate(-9deg)}.small .format.json{left:194px;top:190px}.small .format.html{left:264px;transform:rotate(9deg)}.marquee .plaque{left:88px;top:143px;width:389px;height:182px;padding:26px 39px}.marquee .route{left:605px;top:-250px;width:840px;height:1030px;transform:rotate(19deg)}.marquee .route.two{left:685px;top:-160px;width:710px;height:850px;transform:rotate(19deg)}.marquee .format{width:143px;height:182px;font-size:27px;align-items:flex-start;padding-top:39px}.marquee .format.csv{left:803px;top:187px;transform:rotate(-13deg)}.marquee .format.json{left:982px;top:115px;transform:rotate(5deg)}.marquee .format.html{left:1142px;top:230px;transform:rotate(14deg)}.marquee .tagline{position:absolute;left:88px;top:361px;color:#e6efff;font-size:24px;letter-spacing:-.4px}.marquee .connector{position:absolute;left:530px;top:261px;width:187px;border-top:2px solid #75adee88}.marquee .connector:after{content:'›';position:absolute;right:-4px;top:-25px;font-size:36px;color:#75adee}.thumbnail .copy{top:216px;width:594px}.thumbnail h1{font-size:78px;line-height:1.05;letter-spacing:-3px}.thumbnail .frame{right:24px;width:510px;top:37px;height:646px;transform:rotate(2deg)}.thumbnail .capture img{width:432px}.thumbnail .rail{width:563px}.thumbnail .proof{margin-top:26px}.thumbnail .proof span{font-size:18px}.thumbnail .foot{bottom:44px}.bmc{background:#e9eef4}.bmc .background{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}.bmc .shade{position:absolute;inset:0;background:linear-gradient(90deg,#eff3f8 0%,#eff3f8f5 35%,#eff3f890 57%,transparent 75%)}.bmc .brand{left:62px;top:26px;width:183px}.bmc h1{position:absolute;left:298px;top:39px;margin:0;font-size:45px;letter-spacing:-1.5px}.bmc p{position:absolute;left:300px;top:108px;font-size:23px;max-width:none;color:#45617f}</style>`;
const html = content => `<!doctype html><html lang="en"><meta charset="utf-8">${base}<body>${content}</body></html>`;
const brand = '<img class="brand" src="../brand/logo.svg" alt="HistoryOut">';
const bar = title => `<div class="bar"><i></i><i></i><i></i><span>${title}</span></div>`;
const scenes = [
  {file:'01-export-browser-history.png',kicker:'Export your browser history',title:'Your history.<br><b>Your file.</b>',description:'Export to CSV, JSON or HTML. Keep a useful file you can open, work with or share.',proof:['CSV','JSON','HTML'],image:'export-controls-card.png',layout:'export'},
  {file:'02-customize-your-export.png',kicker:'Make the file yours',title:'Your dates.<br><b>Your detail.</b>',description:'Choose a date range and the columns that belong in your export.',proof:['Custom dates','Choose your columns'],image:'custom-date-controls.png',layout:'customize'},
  {file:'03-readable-html-export.png',kicker:'Readable HTML, included',title:'Export it.<br><b>Open it later.</b>',description:'Keep a readable local file with clickable links. Revisit the useful pages whenever you need them.',proof:['Actual HTML download','Clickable links'],image:'05-real-html-export.png',wide:true},
  {file:'04-filter-your-results.png',kicker:'Keep the matching pages',title:'Less noise.<br><b>Useful results.</b>',description:'Search and filter before you export. Keep the pages that matter to your work.',proof:['Title and URL search','Website filters'],image:'02-project-focus.png'},
  {file:'05-revisit-and-repeat.png',kicker:'Useful extras, included',title:'Pick it up<br><b>tomorrow.</b>',description:'Daily recaps and saved views help you revisit useful pages and repeat your export settings.',proof:['Daily recap','Saved views'],image:'01-daily-focus.png',strip:'saved-view-strip.png'}
];
function panelContent(s){
 if(s.layout==='export')return `<div class="export-stack"><img src="/launch/assets/raw/export-controls-card.png" alt="Actual export controls"><img class="download" src="/launch/assets/raw/export-download-button.png" alt="Download matching history"><div class="result-label">history-export.html</div><div class="file-crop"><img src="/launch/assets/raw/html-links-hero.png" alt="Actual HTML file with links"></div></div>`;
 if(s.layout==='customize')return `<div class="custom-stack"><img class="dates" src="/launch/assets/raw/custom-date-controls.png" alt="Actual custom dates"><div class="column-area"><div><strong>Choose your<br>columns.</strong><span>Keep the detail<br>you need.</span></div><img src="/launch/assets/raw/column-options.png" alt="Actual column options"></div></div>`;
 return `${s.strip?`<img src="/launch/assets/raw/${s.strip}" alt="Saved view">`:''}<img src="/launch/assets/raw/${s.image}" alt="${escape(s.kicker)}">`;
}
function screenshot(s){return html(`<main class="art ${s.wide?'wide':''}"><div class="rail"></div>${brand}<section class="copy"><div class="kicker">${s.kicker}</div><h1>${s.title}</h1><p>${s.description}</p><div class="proof">${s.proof.map(p=>`<span>${p}</span>`).join('')}</div></section><div class="frame">${bar(s.wide?'Actual downloaded HTML file':'HistoryOut · Actual interface')}<div class="capture ${s.strip?'stacked':''}">${panelContent(s)}</div></div><div class="foot">Free · No account · On your device<div class="note">${s.note||'Actual interface. Fictional browsing history for this demo.'}</div></div></main>`);}
function promo(size){return html(`<main class="art promo ${size}"><div class="route"></div>${size==='marquee'?'<div class="route two"></div><div class="connector"></div>':''}<div class="plaque"><img src="../brand/logo.svg" alt="HistoryOut"></div><div class="format csv">CSV</div><div class="format json">JSON</div><div class="format html">HTML</div>${size==='marquee'?'<div class="tagline">Export your history. Keep what matters.</div>':''}</main>`);}
function thumbnail(){return html(`<main class="art thumbnail"><div class="rail"></div>${brand}<section class="copy"><div class="kicker">Free browser history export</div><h1>Export your<br><b>history.</b></h1><div class="proof"><span>CSV</span><span>JSON</span><span>HTML</span></div></section><div class="frame">${bar('HistoryOut · Actual interface')}<div class="capture"><img src="/launch/assets/raw/04-clean-export.png" alt="Export formats and options"></div></div><div class="foot">Choose dates, pages and columns.</div></main>`);}
async function render(page,origin,name,content,width,height,destination){await fs.writeFile(path.join(ROOT,'source',name+'.html'),content);await page.setViewportSize({width,height});await page.goto(origin+'/launch/store-kit/source/'+name+'.html');await page.locator('img').evaluateAll(images=>Promise.all(images.map(img=>img.decode())));const data=await page.screenshot();await sharp(data).flatten({background:'#ffffff'}).removeAlpha().png().toFile(path.join(ROOT,destination));}

function websiteHero(){return `<!doctype html><html><meta charset="utf-8"><style>*{box-sizing:border-box}body{margin:0;width:1600px;height:1350px;overflow:hidden;background:#faf9f6;color:#122c48;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif}.card{position:absolute;background:#fff;border:2px solid #d8e2ee;border-radius:28px;overflow:hidden;box-shadow:0 26px 70px #183c6819}.controls{left:52px;top:40px;width:1480px;height:649px;padding:30px 62px}.wordmark{width:225px;height:90px;object-fit:contain}.actual{position:absolute;right:62px;top:59px;font-size:23px;color:#6a7b92;letter-spacing:.1px}.formats{width:1352px;height:auto;margin-top:25px;display:block}.download{width:1352px;height:auto;display:block;margin-top:32px}.result{left:275px;top:728px;width:1260px;height:576px;background:#f5f7fb}.filebar{height:67px;background:#fff;border-bottom:2px solid #dce5ef;padding:18px 30px;font-size:25px;display:flex;align-items:center;gap:17px;color:#335675}.filebar span{font-size:19px;margin-left:auto;color:#6b809b}.file{width:1260px;height:auto;display:block}.arrow{position:absolute;left:121px;top:865px;width:94px;height:94px;display:grid;place-items:center;border-radius:50%;background:#185adb;color:white;font-size:59px;font-weight:500}</style><body><section class="card controls"><img class="wordmark" src="../brand/logo.svg" alt="HistoryOut"><span class="actual">Your chosen format. Ready to download.</span><img class="formats" src="/launch/assets/raw/export-format-controls.png" alt="Actual CSV, JSON and HTML export controls"><img class="download" src="/launch/assets/raw/export-download-button.png" alt="Actual export button"></section><div class="arrow">↓</div><section class="card result"><div class="filebar">history-export.html<span>Actual downloaded file</span></div><img class="file" src="/launch/assets/raw/html-links-hero.png" alt="A readable HTML history file with clickable links"></section></body></html>`;}
async function assetGallery(){await fs.writeFile(path.join(ROOT,'index.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><title>HistoryOut export launch assets</title><style>body{max-width:1280px;margin:50px auto;padding:20px;font:18px/1.6 system-ui;background:#faf9f6;color:#122c48}img{display:block;max-width:100%;margin:20px 0 60px}a{color:#185adb}</style><h1>HistoryOut export launch assets</h1><p>Actual product controls and fictional data. The optional support strip is omitted for marketing captures only.</p>${scenes.map(s=>`<h2>${s.kicker}</h2><img src="screenshots/${s.file}" alt="${escape(s.kicker)}">`).join('')}<h2>Export thumbnail</h2><img src="youtube/thumbnail-1280x720.png" alt="Export browser history"><h2>Website hero</h2><img src="../assets/website-export-hero.png" alt="Export controls and an actual HTML file"></html>`);}

async function files(dir){const out=[];for(const e of await fs.readdir(dir,{withFileTypes:true})){const p=path.join(dir,e.name);if(e.isDirectory())out.push(...await files(p));else out.push(p);}return out;}
async function main(){
  for(const dir of ['brand','source','screenshots','promotional','youtube','chrome','edge','support'])await fs.mkdir(path.join(ROOT,dir),{recursive:true});
  // These are byte-identical originals, preserved at the user's request.
  const originalIcon=execFileSync('git',['show','0272441:extension-unpacked/icons/icon128.png']);
  await fs.writeFile(path.join(ROOT,'brand','icon128.png'),originalIcon);
  for(const size of [16,32,48])await fs.writeFile(path.join(ROOT,'brand',`icon${size}.png`),execFileSync('git',['show',`0272441:extension-unpacked/icons/icon${size}.png`]));
  await sharp(path.join(ROOT,'brand','logo.svg')).resize({width:861}).png().toFile(path.join(ROOT,'brand','wordmark-861.png'));
  // Edge accepts 128px. Preserve the exact original rather than invent a 300px redraw.
  for(const name of await fs.readdir(path.join(ROOT,'screenshots')))if(name.endsWith('.png')&&!scenes.some(s=>s.file===name))await fs.rm(path.join(ROOT,'screenshots',name));
  const server=await startServer();const browser=await chromium.launch({headless:true});
  try{const page=await browser.newPage();for(const s of scenes)await render(page,server.origin,s.file.replace('.png',''),screenshot(s),1280,800,'screenshots/'+s.file);
    await render(page,server.origin,'small-promo',promo('small'),440,280,'promotional/small-promo-440x280.png');
    await render(page,server.origin,'marquee',promo('marquee'),1400,560,'promotional/marquee-1400x560.png');
    await render(page,server.origin,'youtube-thumbnail',thumbnail(),1280,720,'youtube/thumbnail-1280x720.png');
    await render(page,server.origin,'website-export-hero',websiteHero(),1600,1350,'../assets/website-export-hero.png');
    await fs.copyFile(path.join(ROOT,'youtube/thumbnail-1280x720.png'),path.join(ASSETS,'social-card.png'));
    const background=path.join(ROOT,'support','background.png');
    try{await fs.access(background);await render(page,server.origin,'support-cover',html(`<main class="art bmc"><img class="background" src="../support/background.png" alt=""><div class="shade"></div>${brand}<h1>Small tools. Useful days.</h1><p>Free to use. Supported by you.</p></main>`),1600,400,'support/buy-me-a-coffee-cover-1600x400.png');}catch(error){if(error.code!=='ENOENT')throw error;}
  }finally{await browser.close();await server.close();}
  if(process.argv.includes('--assets-only')){for(const target of ['chrome','edge']){const dir=path.join(ROOT,target,'screenshots');await fs.mkdir(dir,{recursive:true});for(const name of await fs.readdir(dir))if(name.endsWith('.png'))await fs.rm(path.join(dir,name));for(const scene of scenes)await fs.copyFile(path.join(ROOT,'screenshots',scene.file),path.join(dir,scene.file));}await assetGallery();console.log('Marketing assets rendered without packaging:',ROOT);return;}
  const shared=[...scenes.map(s=>'screenshots/'+s.file),'promotional/small-promo-440x280.png','promotional/marquee-1400x560.png','brand/icon128.png'];
  for(const target of ['chrome','edge']){
    for(const name of await fs.readdir(path.join(ROOT,target,'screenshots')))if(name.endsWith('.png')&&!scenes.some(s=>s.file===name))await fs.rm(path.join(ROOT,target,'screenshots',name));
    for(const relative of shared){const dest=path.join(ROOT,target,relative);await fs.mkdir(path.dirname(dest),{recursive:true});await fs.copyFile(path.join(ROOT,relative),dest);}
    for(const name of await fs.readdir(path.join(ROOT,target)))if(/^historyout-.*\.zip$/.test(name))await fs.rm(path.join(ROOT,target,name));
    if(process.argv.includes('--package-extension')){
      const version=JSON.parse(await fs.readFile('extension-unpacked/manifest.json')).version;
      const archive=path.resolve(`releases/historyout-${version}-${target}.zip`);
      for(const file of ['bundle.js','background.js','icons/icon128.png']){
        if(sha(execFileSync('unzip',['-p',archive,file]))!==sha(await fs.readFile(path.join('extension-unpacked',file))))throw new Error(`Stale ${target} package: ${file}. Run npm run pack again.`);
      }
      await fs.copyFile(archive,path.join(ROOT,target,`historyout-${version}-${target}.zip`));
    }
  }
  const metadata=[];for(const file of await files(ROOT)){const relative=path.relative(ROOT,file);if(['manifest.json','archives.json','index.html'].includes(relative)||relative.startsWith('historyout-')||relative.startsWith('source/'))continue;const data=await fs.readFile(file);const item={file:relative,bytes:data.length,sha256:sha(data)};if(/\.(png|ico|svg)$/.test(file)){try{const m=await sharp(data).metadata();Object.assign(item,{width:m.width,height:m.height,hasAlpha:m.hasAlpha});}catch{}}
    metadata.push(item);
  }
  const inputCaptures=[];for(const name of [...scenes.map(s=>s.image),'saved-view-strip.png'])inputCaptures.push({file:'launch/assets/raw/'+name,sha256:sha(await fs.readFile(path.join(ASSETS,'raw',name)))});
  await fs.writeFile(path.join(ROOT,'manifest.json'),JSON.stringify({generated:new Date().toISOString(),extensionVersion:JSON.parse(await fs.readFile('extension-unpacked/manifest.json')).version,bundleSHA256:sha(await fs.readFile('extension-unpacked/bundle.js')),backgroundSHA256:sha(await fs.readFile('extension-unpacked/background.js')),originalIconGitRef:'0272441',originalIconSHA256:sha(originalIcon),screenshots:'Actual extension UI with fictional QA history. Original icon and wordmark preserved; all compositions are new.',inputCaptures,files:metadata},null,2)+'\n');
  for(const target of ['chrome','edge']){const archive=path.join(ROOT,`historyout-${target}-store-kit.zip`);await fs.rm(archive,{force:true});execFileSync('zip',['-qr',archive,target],{cwd:ROOT});}
  const archiveHashes=[];for(const target of ['chrome','edge']){const name=`historyout-${target}-store-kit.zip`;const data=await fs.readFile(path.join(ROOT,name));archiveHashes.push({file:name,bytes:data.length,sha256:sha(data)});}await fs.writeFile(path.join(ROOT,'archives.json'),JSON.stringify(archiveHashes,null,2)+'\n');
  await assetGallery();
  console.log('HistoryOut store kit:',ROOT);
}
main().catch(error=>{console.error(error);process.exitCode=1;});
